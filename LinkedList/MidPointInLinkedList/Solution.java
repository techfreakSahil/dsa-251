package LinkedList.MidPointInLinkedList;

public class Solution {
    public ListNode midPoinListNode(ListNode head) {
        ListNode slow = head;
        ListNode fast = head.next;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }
        return slow;
    }
}
